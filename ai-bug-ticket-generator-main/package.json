{
  "name": "ai-bug-ticket-generator",
  "version": "1.0.0",
  "private": true,
  "description": "AI Bug Ticket Generator with React frontend, Express backend, Groq LLM, and optional Jira ticket creation.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "start": "npm run start --workspace backend",
    "install:all": "npm install --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
