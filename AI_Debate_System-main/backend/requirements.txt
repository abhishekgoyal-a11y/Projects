fastapi==0.115.6
uvicorn[standard]==0.34.0

# Pinned where required; relaxed for higher compatibility with other installed packages.
# For isolation and to avoid conflicts, prefer installing inside a virtualenv.

pydantic==2.10.6
pydantic-settings==2.7.1

httpx==0.28.1

# Core language/ML libs: use minimum versions to reduce resolver conflicts
langgraph>=0.2.60
langchain>=0.3.14
langchain-groq>=0.2.3

groq>=0.13.1

tavily-python>=0.5.0

python-dotenv>=1.0.1

pytest>=8.3.4